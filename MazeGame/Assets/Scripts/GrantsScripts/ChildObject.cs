using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ChildObject : MonoBehaviour
{
    //Place this script on an object to find it as a type rather than searching for it via tag
    //This will save memory and boost performance
}
